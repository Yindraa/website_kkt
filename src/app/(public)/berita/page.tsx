// src/app/(public)/berita/page.tsx

// 'use client' tidak diperlukan jika ini adalah Server Component (default)

/** * Ini adalah Komponen Halaman (Page Component)
 * yang akan di-render untuk rute /berita.
 */
export default function BeritaPage() {
  return (
    <main>
      {/* Konten akan ditambahkan di sini nanti */}
      <h1>Halaman Berita (Kosong Sementara)</h1>
    </main>
  );
}
